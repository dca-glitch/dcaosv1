/**
 * Product UI adapters for design-system alert / feedback primitives.
 * Prefer importing from `components/ui` rather than design-system directly.
 */
export { default as Alert, Toast, SuccessState } from "../../design-system/components/Alert";
export type { AlertVariant } from "../../design-system/components/Alert";
