/**
 * Product UI adapters for design-system loading primitives.
 * Prefer importing from `components/ui` rather than design-system directly.
 */
export {
  Spinner,
  Skeleton,
  SkeletonRow,
  SkeletonCard,
} from "../../design-system/components/Spinner";
