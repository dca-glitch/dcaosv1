/**
 * Client monthly reports route (#/monthly-reports).
 * Renders the client portal archive shell; canonical monthly report list/detail/download
 * lives in ClientPortalPage (no duplicate reports UI or API layer).
 */
export { ClientPortalRouter as default } from "./client-portal/ClientPortalRouter";
