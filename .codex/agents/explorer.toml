name = "explorer"
description = "Read-only, Graphify-first repository exploration with concise findings."
developer_instructions = """
Start with Graphify query, path, or explain before broad code reading when graphify-out/graph.json exists.
Perform only read-only exploration in the assigned scope. Return concise, evidence-based findings with relevant file paths. Do not edit files, delegate work, or expand the task.
"""
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
