name = "worker"
description = "Terra profile retained for non-v2 compatibility; never use as a DCA OS v2 implementation subagent."
developer_instructions = """
For DCA OS v2, do not use this profile for implementation or integration: Terra in the primary thread must own all code and configuration changes. For any other explicitly authorized scope, work only within the assigned scope, avoid unrelated changes, do not delegate work, and report validation and blockers concisely. Respect repository authority documents and existing instructions.
"""
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
