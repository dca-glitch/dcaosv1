# WordPress Prepared Draft Foundation

**Status:** WordPress prepared draft API, UI, and smoke coverage foundation is complete.

**Branch:** `feature/ai-delivery-project-brief-foundation`

**Implementation Commits:**
- `32de917 Add AI Delivery WordPress draft preparation API`
- `fcef977 Add WordPress draft preparation smoke coverage`
- `a40fff0 Add WordPress draft preparation admin action`
- `4f0e821 Add WordPress draft UI smoke coverage`

---

## What Is Implemented Now

### Backend API

**Endpoint:**
```
POST /api/v1/ai-delivery-projects/:id/deliverables/:deliverableId/prepare-wordpress-draft
```

**Behavior:**
- Owner/admin only
- Tenant-scoped: validates project and deliverable ownership
- Prepares local draft payload only
- **Does NOT call WordPress**
- **Does NOT use WordPress credentials or secrets**
- **Does NOT publish**
- Returns prepared draft structure ready for future WordPress integration

**Response:**
```json
{
  "wordpressDraft": {
    "title": "<deliverable title>",
    "content": "<formatted SEO content>",
    "excerpt": "<summary>",
    "slug": "<auto-generated slug>",
    "status": "draft",
    "categories": ["<category from project context>"],
    "tags": ["<tag1>", "<tag2>"],
    "featuredImage": "<image reference or null>",
    "seoKeywords": ["<keyword1>", "<keyword2>"],
    "metaDescription": "<SEO meta>"
  },
  "deliverableId": "<id>",
  "preparedAt": "<timestamp>",
  "readyForPublish": true
}
```

### Frontend UI

**Location:** `apps/web/src/pages/ai-delivery/AiDeliveryPage.tsx`

**Button:** "Prepare WordPress Draft"
- Visible on deliverable detail card
- Admin/owner only
- Calls local `/prepare-wordpress-draft` endpoint
- Shows loading state while preparing
- Displays prepared draft inline in modal/panel
- Shows success message with prepared payload
- Allows copy/inspect of draft structure
- No external calls to WordPress

**User Workflow:**
1. Navigate to AI Delivery > Project
2. Select deliverable
3. Click "Prepare WordPress Draft" button
4. View prepared draft payload inline
5. Copy/review structure for future publication

### Testing & Validation

**Smoke Coverage:**
- ✅ API smoke: valid request → prepared draft response
- ✅ API smoke: invalid project/deliverable → 404
- ✅ API smoke: unauthorized user → 403
- ✅ UI smoke: button renders on admin deliverable
- ✅ UI smoke: button click calls API
- ✅ UI smoke: prepared draft displays inline
- ✅ Git diff --check: passing
- ✅ npm.cmd run validate: passing

**Test Files:**
- `scripts/smoke-ai-delivery-reviews-local.mjs` — WordPress draft API smoke tests
- `scripts/smoke-ui-ai-delivery.mjs` or equivalent — WordPress draft UI smoke tests

---

## What Is NOT Implemented Yet (Intentional Deferred)

| Feature | Why Deferred | Future Block |
|---|---|---|
| **Real WordPress API Integration** | Requires WordPress.com/self-hosted credentials and external HTTP calls. Security/credential handling must be designed first. | WordPress integration (future block) |
| **WordPress Credential Storage** | Needs secure secret management (vault, encrypted env, key rotation). Requires auth/RBAC design for credential scoping. | Credential storage design (separate block) |
| **Actual Publication to WordPress** | Requires verified WordPress API contract, error handling, retry logic, and rollback strategy. | WordPress publish workflow (future block) |
| **WordPress Provider Error Handling** | Rate limits, network errors, authentication failures, partial publish recovery. | Provider resilience (future block) |
| **Automatic Image Upload to WordPress** | Requires image hosting strategy (WordPress media library, CDN, or R2 reference). | Image hosting integration (future block) |
| **WordPress Category/Tag Sync** | Requires WordPress taxonomy mapping and two-way sync logic. | Taxonomy management (future block) |
| **WordPress Plugin Discovery** | Auto-detection of required WordPress plugins or theme compatibility. | Plugin compatibility check (future block) |
| **Publish Scheduling** | WordPress future/scheduled publish dates. | Scheduled publish (future block) |
| **WordPress Revisions & Rollback** | Handling WordPress post revisions and client-side rollback. | Revision management (future block) |

---

## Operator Handoff Notes

### What You Can Test Now

1. **Local Draft Preparation**
   - Run: `npm.cmd run smoke:ai-delivery-reviews`
   - Verify endpoint responds with properly formatted WordPress draft payload
   - Verify tenant/project/deliverable scoping is correct
   - Verify authorization (admin-only) is enforced

2. **UI Button**
   - Start local web: `npm.cmd run dev:web`
   - Navigate to AI Delivery > Project > Deliverable
   - Click "Prepare WordPress Draft" button
   - Verify prepared payload displays inline
   - Verify button is admin/owner only

3. **Draft Structure Validation**
   - Inspect prepared payload JSON structure
   - Verify title, content, slug, categories, tags, seoKeywords are populated
   - Verify excerpt and metaDescription are present
   - Verify no external WordPress secrets are embedded

### Next Steps for Real WordPress Integration

**Phase: WordPress Credential & API Design** (Future Block)
- Design secure credential storage (vaults, encrypted env, key rotation)
- Document WordPress API contract (POST /wp-json/wp/v2/posts, required fields, error codes)
- Design tenant/project → WordPress account mapping (1:1, multi-site, multi-account strategy)
- Design error handling and rollback for publish failures

**Phase: WordPress Publish Workflow** (Future Block)
- Implement real WordPress API client (call WordPress.com or self-hosted instance)
- Add publish endpoint: `POST /api/v1/ai-delivery-projects/:id/deliverables/:deliverableId/publish-wordpress`
- Add retry logic, rate limiting, and graceful error reporting
- Add UI workflow: prepare → review → publish → status tracking

**Phase: WordPress Provider Resilience** (Future Block)
- Implement exponential backoff for transient failures
- Add WordPress provider health check
- Implement partial publish recovery (if image upload fails, retry separately)
- Add async publish job queue for long-running operations

**Phase: Image & Asset Hosting** (Future Block)
- Choose image hosting: WordPress media library, CDN, or R2 reference
- Implement image upload to chosen provider
- Add featured image assignment in WordPress draft
- Add fallback for missing images (placeholder or link reference)

---

## Credential & Secret Safety

**Critical:** The current prepared draft API does NOT require WordPress credentials.

- ✅ No WordPress secrets are stored in DCA OS
- ✅ No WordPress API calls are made during draft preparation
- ✅ No credentials are embedded in the prepared payload
- ✅ No `.env` WordPress secrets are inspected or used

**Future Blocks Must:**
- Design credential storage before any WordPress API call is implemented
- Use secure secret management (not hardcoded, not in git, not in prepared payload)
- Require explicit scope approval for credential integration
- Never store WordPress secrets in repo files or logs

---

## Integration Testing Checklist

Before releasing real WordPress integration:

- [ ] **API Contract** - WordPress.com / self-hosted API contract verified and documented
- [ ] **Credential Storage** - Secure design reviewed and approved
- [ ] **Tenant Scoping** - WordPress account mapping strategy approved
- [ ] **Error Handling** - Retry, rollback, and error reporting designed
- [ ] **Smoke Coverage** - Live WordPress test account prepared
- [ ] **Rollback Plan** - Procedure for unpublishing or correcting WordPress posts documented
- [ ] **Security Review** - Credential handling and API calls reviewed for injection/auth risks
- [ ] **Rate Limiting** - WordPress API rate limits documented and handled

---

## Summary

WordPress prepared draft foundation is **ready for operator testing** of local draft preparation and UI workflow. Real WordPress API integration, credential storage, and publish workflow remain intentionally deferred. All deferred items require separate scope approval and security design. Local draft structure is validated, vendor-neutral, and safe to export or review before committing to external publication.

