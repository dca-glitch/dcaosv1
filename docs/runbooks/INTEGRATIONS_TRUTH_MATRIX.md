# Integrations Truth Matrix

**Status:** Docs-only audit deliverable. Config-shape/code-inspection based. No live provider API calls were made to produce this document.

**Source of truth:** [`docs/STATUS.md`](../STATUS.md) · [`docs/operator/ENV_READINESS_INVENTORY.md`](../operator/ENV_READINESS_INVENTORY.md) · [`docs/runbooks/EXTERNAL_INTEGRATIONS_READINESS.md`](./EXTERNAL_INTEGRATIONS_READINESS.md) · [`docs/operator/deferred-scope-register.md`](../operator/deferred-scope-register.md) · [`docs/architecture/AI_MODEL_POLICY.md`](../architecture/AI_MODEL_POLICY.md) (AI provider/model policy — provider decisions pending owner approval)

**Rule:** Every row's "Production proof" and "Staging proof" columns reflect actual evidence recorded in `docs/STATUS.md` as of 2026-07-10 (G228 final integration) — not aspirational status. "Not proven" for staging/production means exactly that. G89-G227 added local/no-IO foundations only; no integration claims staging or production live-proven status.

**G78 docs audit (2026-07-10):** Monthly report metrics and WordPress tier labels refreshed in owned integration docs. Headline confirmations: (1) monthly reports — local baseline is MANUAL/snapshot placeholder (`placeholderOnly: true`); live GA/GSC is config-shape-only with no OAuth/token implementation; (2) WordPress — draft prep local-proven; live draft proof plan-only; publish frozen by default.

**G88 consolidation (2026-07-10):** Correct gate map recorded for G79-G88. G79 is implemented locally for monthly cap aggregation of live `COMPLETED` rows; G80 is policy-only (`actualCostUsd` remains `null` unless trusted provider cost is available); G81-G87 are planning/prep only. No staging/prod live proof, live AI call, email send, Google OAuth/sync, WordPress HTTP call, image-provider call, R2 IO, commit, push, or deploy was performed by this consolidation.

**G148 integration (2026-07-10):** Labels updated for G89-G147 lane outcomes: R2 no-IO readiness/proof stages/storage-key guards, notifications taxonomy/no-send foundation, GA/GSC helper/config foundation, WordPress draft payload + publish-freeze-before-fetch tests, image compliance policy helpers, Client Portal FINAL guards, pack/future-module contracts, AI budget reporting contract, and operator/security docs. Staging and production proof columns remain **Not proven** unless a prior recorded proof explicitly says otherwise.

**G228 integration (2026-07-10):** G149-G227 expanded local foundations (R2 proof-stage/redaction/URL policy, notification taxonomy V2 + persistence design, GA/GSC period/source-truth/output guards, WordPress slug/freeze/redaction/image-inclusion, image approval-loop helpers, Client Portal boundary tests, Puriva pack entitlements, future-module contracts, operator security/inventory/roadmap). **No staging or production proof column changes.** No live provider call, R2 IO, OAuth/sync, WordPress HTTP, email send, image-provider call, staging/prod probe, commit, push, or deploy was performed.

---

| Integration | Implementation status | Local proof | Staging proof | Production proof | Required secrets/env (names only) | Client / Puriva dependency | Risk | Next proof step | Owner approval required |
|---|---|---|---|---|---|---|---|---|---|
| **OpenRouter (AI text)** | Implemented; local deterministic gateway is default; OpenRouter path coded and config-validated; G79 local ledger aggregation includes live `COMPLETED` rows | PASS local deterministic (default) + **PASS local controlled live OpenRouter** (G71e/G77b; `anthropic/claude-haiku-4.5`; COMPLETED ledger row `5d8d635c-ced0-4a14-9b33-839e1fdee508`; G79 aggregation local unit proof; `actualCostUsd=null`) | Not proven | Not proven | `OPENROUTER_API_KEY`, `OPENROUTER_TEXT_PRIMARY_MODEL`, optional secondary/reviewer/long-context models, `AI_TEXT_GATEWAY=openrouter` | Puriva blocker: staging/production live AI text re-proof (local complete only) | Medium — cost/rate-limit exposure if enabled without budget guardrail review | Re-run bounded live proof on staging after owner approval; do not claim invoice cost until trusted provider-cost ingestion exists per G80 policy | Yes (staging re-proof + model selection) |
| **Image generation** | Scaffold only — workflow image-set prepare/refresh + `AiDeliveryArticleImage` CRUD; **no live provider wired** | N/A (no provider) | N/A | N/A | Not yet defined — see [`IMAGE_GENERATION_PROOF.md`](./IMAGE_GENERATION_PROOF.md) | Puriva blocker: provider research (#4) + staging proof (#5) + R2 image bytes | Low today; High once wired without policy gate | Finalize provider/fallback + cost caps in [`AI_MODEL_POLICY.md`](../architecture/AI_MODEL_POLICY.md) §2.1/§2.3 (currently pending owner approval); wire disabled-safe provider; prove per `IMAGE_GENERATION_PROOF.md` Phase C | Yes (research + policy + live proof) |
| **WordPress (draft-prep)** | Implemented and local-proven; G110-G113 add draft payload, credential-shape checks, and publish-freeze-before-fetch coverage; draft creation/handoff only | PASS (workflow brief + AI Delivery draft-prep smokes; focused lane tests 5/5) | Not proven | Not proven | N/A (draft-prep does not require publish env) | Required for Puriva Launch (draft/handoff only; auto-publish stays deferred) | Low for draft-prep; Medium for future publish | No action needed for draft-prep; a **live draft proof plan** (docs-only, no live call yet) is written up in [`WORDPRESS_DRAFT_PROOF.md`](./WORDPRESS_DRAFT_PROOF.md) §6 covering title/body/meta parity, approved-image-only attach, alt/caption/social preview, idempotency, cleanup marker, and disabled-safe restore | No (draft-prep plan/docs); Yes (executing a live draft proof session or publish) |
| **WordPress (live draft proof — planned, not executed)** | Plan-only; no live WordPress HTTP call has been made under this row | N/A (planning artifact, not a code smoke) | Not proven | Not proven | Same as draft-prep, plus `WORDPRESS_PUBLISH_ENABLED=true` temporarily on a staging-only site during the session | Required before any Puriva WordPress claim can move from "prepared locally" to "verified against a real site" | Medium — depends on unresolved gaps: no `altText`/`caption`/`socialPreview` fields on `AiDeliveryArticleImage`, no idempotency/dedupe key on `PublicationLog`, no code-level filter enforcing approved-image-only attach (all documented as manual-check-or-schema-change decisions in `WORDPRESS_DRAFT_PROOF.md` §6.3–§6.5) | Resolve or explicitly accept (manual-check-only) the §6.3–§6.5 gaps, then run one owner-approved staging-only live draft proof session per `WORDPRESS_DRAFT_PROOF.md` §6, record evidence per §10.2, and restore disabled-safe mode immediately after | Yes (session itself; separate from this docs-only row) |
| **WordPress (publish)** | Implemented behind a disabled-by-default gate; publish never occurs unless explicitly enabled | PASS disabled-safe (`smoke:wordpress-publish:local`); optional open-gate probe exists but is not the default | Not proven | Not proven | `WORDPRESS_PUBLISH_ENABLED=true`, `CREDENTIAL_ENCRYPTION_MASTER_KEY` | Explicitly deferred — auto-publish remains out of scope for Puriva Launch v1 | High if enabled prematurely (irreversible public content) | Do not schedule unless/until a separate explicit publish-proof block is approved | Yes |
| **GA4 / Google Search Console** | Config/readiness helper layer implemented (env presence only); G103-G109 add local helpers and FINAL/manual-metrics visibility support; **no OAuth consent/callback route, no token exchange, no token storage model, and no token refresh logic exist in this repo** — see [`MONTHLY_REPORT_LIVE_DATA_PROOF.md`](./MONTHLY_REPORT_LIVE_DATA_PROOF.md) §3.1a | PASS disabled-safe (`smoke:external-integrations-readiness:local`; lane focused tests 26/26 and API check PASS) | Not proven | Not proven | `GA4_GSC_SYNC_ENABLED=true`, `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET` | Puriva blocker: "GA/GSC live proof"; Monthly Report Flow v1 depends on this for real (non-placeholder) metrics | Medium — OAuth misconfiguration risk if rushed; **higher near-term risk is the missing token storage/encryption design**, which must exist before any consent flow is attempted | 1) Design + owner-approve OAuth token storage (encrypted at rest, scoped, refreshable) as its own schema/code block; 2) complete OAuth consent screen setup; 3) run one bounded read-only GA/GSC pull against a test property before any client property is connected | Yes |
| **R2 (private storage)** | Implemented; disabled-safe by default; G89-G93 add no-IO readiness, proof stages, and storage-key guard coverage; hand-rolled SigV4 signer, no live bucket ever exercised | PASS disabled-safe (`smoke:r2-byte-roundtrip:local`, `smoke:r2-storage-boundary:local`, unit + integration tests; lane readiness 30/30 and focused tests 18/18); document roundtrip + signed URL + disabled fail-closed covered — image-variant byte roundtrip and full two-tenant denial still deferred | Not proven | Not proven | `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, optional `R2_ENDPOINT`/`R2_PUBLIC_BASE_URL` | Puriva blocker: "R2/storage live proof" | Medium — untested signer against real endpoint; see `STORAGE_R2_PROOF.md` | Provision a staging-only bucket and run the full checklist in `STORAGE_R2_PROOF.md` §6 | Yes |
| **Email / notifications (Resend)** | Implemented as outbound email/outbox foundation; G94-G102 add pure taxonomy/mapping/policy/no-send adapter/template foundations; local mode reports `SKIPPED` without a real key; there is still **no in-system notification model** and no live email proof — see [`EMAIL_NOTIFICATIONS_PROOF.md`](./EMAIL_NOTIFICATIONS_PROOF.md) §2 | PASS disabled-safe/no-send planning and local outbox proof; lane focused tests 20/20; no live send | Not proven | Not proven | `RESEND_API_KEY` | Puriva blocker: "Transactional notification proof (in-system + email)" | Medium — deliverability/reputation risk if first live send is unplanned; product risk if outbound email is mistaken for an in-system inbox | Build/prove in-system notification MVP before client-facing launch claim; send one bounded test transactional email to an owner-controlled inbox only after approval (plan in `EMAIL_NOTIFICATIONS_PROOF.md` §4) | Yes |
| **PDF / export (monthly reports, content plans)** | Implemented and local-proven; no external provider dependency for generation itself | PASS (`smoke:monthly-report:pdf`, `smoke:ai-seo-content-plan-pdf`) | Not proven (staging environment parity not re-verified) | Not proven | None beyond R2 for storage of the generated file | Required for Monthly Report Flow v1 (Puriva) — **local baseline uses MANUAL/snapshot placeholder metrics**; live GA/GSC totals require separate row proof | Low | Re-run the same local smokes once staging deploy proof exists for this commit | No (generation itself); Yes (if storage requires live R2) |
| **Monthly report metrics (MANUAL/snapshot — local baseline)** | Implemented and local-proven; admin snapshot import/approve path; Puriva manual placeholder metrics with `placeholderOnly: true`; G85 separates this from live GA/GSC | PASS (`smoke:monthly-report:metrics`, `smoke:puriva-monthly-report:local`, `smoke:monthly-metrics-import:browser`) | Not proven | Not proven | None for manual path | Required for Puriva Launch local closeout; **not a substitute for live GA/GSC** | Low if placeholder disclaimer preserved; High if presented as live traffic | Prove per `PURIVA_MANUAL_METRICS_V1_GATE.md`; never promote FINAL with placeholder totals without client-safe disclaimer; live GA/GSC remains separate owner-approved proof | No (manual path) |
| **Google Docs live export** | Planning service implemented (`google-drive-export-planning.service.ts`); one guarded live-probe smoke exists | Guarded/optional (`smoke:google-drive-export-live:local`) — not run by default | Not proven | Not proven | Google OAuth credentials (see GA/GSC row); Drive-specific scopes | Deferred item, not a hard Puriva blocker per current docs | Medium — OAuth scope creep risk | Confirm exact Drive scopes needed before any live OAuth consent is requested | Yes |
| **Credential encryption (WordPress-type credentials at rest)** | Implemented (`credential-encryption.service.ts`); has its own smoke | PASS (`smoke:credential-encryption:local`, `smoke:credential-master-key-probe:local`) | Not proven | Not proven | `CREDENTIAL_ENCRYPTION_MASTER_KEY` | Prerequisite for WordPress publish proof | Medium — key rotation/loss risk not yet exercised | Confirm key-rotation procedure before any production credential is ever encrypted with a production master key | Yes (before first real credential is stored) |

---

## Summary rules confirmed by code inspection

- Every integration follows the same **disabled → missing_config → configured_shape_ok** three-state pattern, and every readiness category explicitly self-labels as live-calls-deferred (`liveCallsDeferred: true` / equivalents) at the type level in `external-integrations-readiness.service.ts`.
- OpenRouter has **local-only** live-proven status (G77b COMPLETED ledger row); no integration claims staging or production live-proven status — all environment live proofs remain owner-gated, consistent with `G53_PRODUCTION_SAFETY_PLAN.md` §4.2.
- Two gaps for a future small block (not blockers, just completeness): (1) image generation has scaffold but no readiness category yet — add when provider is wired per `IMAGE_GENERATION_PROOF.md`; (2) email/Resend readiness is not yet exposed via the unified `GET /api/v1/integrations/readiness` endpoint — it is only visible via the email outbox smoke.
- **Email notifications audit (2026-07-09):** Full pathway/event audit completed in [`EMAIL_NOTIFICATIONS_PROOF.md`](./EMAIL_NOTIFICATIONS_PROOF.md). Headline finding: of the six Puriva-launch notification events (article ready, image set ready, client approved, client rejected, admin action required, monthly report final, WordPress draft prepared), only client-approved and client-rejected currently reach a real inbox; the rest are either internal-only (`SKIPPED`-only placeholder log) or entirely unwired. No live email was sent to produce that audit.
- **SEC-B1 (2026-07-09):** Legacy `/api/v1/briefs` cross-tenant IDOR fixed locally; regression test `briefs-tenant-boundary.integration.test.ts`. Not a launch integration row but a production safety prerequisite.
- **WordPress live draft proof (2026-07-09):** `WORDPRESS_DRAFT_PROOF.md` §6 documents a pre-execution plan only — no live WordPress call was made to produce it or this matrix update. Three concrete code/schema gaps must be explicitly decided (schema change vs. manual-check-only) before a live session can run: no `altText`/`caption`/`socialPreview` fields on `AiDeliveryArticleImage`, no idempotency/dedupe key on `PublicationLog`/`AiDeliveryWordPressPublishRequest`, and no code-level filter enforcing that only `AiDeliveryDeliverableImageApproval.status = APPROVED` images may attach to a draft.
- **GA/GSC token readiness (2026-07-09, docs-only):** Confirmed by code inspection that GA/GSC readiness is env-presence-only — no OAuth consent/callback route, token storage model, or refresh logic exists in `apps/api` or `packages/data`. This is a harder blocker than "complete OAuth consent screen" alone implies; a token storage/encryption design block must be scoped and approved before any consent flow is attempted. See `MONTHLY_REPORT_LIVE_DATA_PROOF.md` §3.1a.
- **Monthly report metrics tiers (G78, 2026-07-10):** Local Puriva path uses MANUAL/snapshot placeholders (`PURIVA_MANUAL_METRICS_V1_GATE.md`, `placeholderOnly: true`). Live GA/GSC OAuth sync is a separate integration row — config-shape readiness only today; do not represent manual placeholders as live analytics or conflate env-presence with working OAuth.
- **WordPress credential policy (G78, 2026-07-10):** Block 4 encryption foundation merged — encrypted Application Password save allowed locally; live HTTP and publish remain frozen per updated `WORDPRESS_CREDENTIAL_POLICY_DECISION.md` three-tier model.
- **G148 consolidation (2026-07-10):** G89-G147 local foundations are integrated, but do not change any staging/production proof column. R2, notifications/email, GA/GSC, WordPress, image generation, Client Portal staging QA, and production launch proofs remain owner-gated.
- **G228 consolidation (2026-07-10):** G149-G227 local foundations are integrated. Staging/production columns remain **Not proven** for R2, email, GA/GSC, WordPress live draft, image generation, and target-env AI re-proof. Next owner-gated sequence: [`G227_NEXT_30_GATES.md`](../operator/G227_NEXT_30_GATES.md).

## Do NOT call external provider APIs with credentials

This document was produced entirely from code/config inspection. No OpenRouter, WordPress, GA/GSC, R2, or Resend API call was made while producing it.
