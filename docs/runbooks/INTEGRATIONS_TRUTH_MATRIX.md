# Integrations Truth Matrix

> **LIVE GA4/GSC WITHDRAWN (2026-07-13):** Live GA4/GSC OAuth/sync is **withdrawn** — not a Puriva Launch blocker; env vars `GA4_GSC_*` / `GOOGLE_OAUTH_*` **not required**. **Google Drive** remains a separate capability with its own OAuth env needs. MANUAL/snapshot monthly-report metrics remain.

**Status:** Docs-only audit deliverable. Config-shape/code-inspection based. No live provider API calls were made to produce this document.

**Source of truth:** [`docs/STATUS.md`](../STATUS.md) · [`docs/operator/ENV_READINESS_INVENTORY.md`](../operator/ENV_READINESS_INVENTORY.md) · [`docs/runbooks/EXTERNAL_INTEGRATIONS_READINESS.md`](./EXTERNAL_INTEGRATIONS_READINESS.md) · `docs/operator/deferred-scope-register.md` (archived reference; see Git history) · [`docs/architecture/AI_MODEL_POLICY.md`](../architecture/AI_MODEL_POLICY.md) (AI provider/model policy — provider decisions pending owner approval) · [`docs/architecture/AI_POLICY_PROVIDER_ROUTING.md`](../architecture/AI_POLICY_PROVIDER_ROUTING.md) (AI Policy / provider routing architecture — OpenRouter is preferred text broker/adapter, not universal boundary; image/audio use direct adapters under the same policy)

**Rule:** Every row's "Production proof" and "Staging proof" columns reflect actual evidence recorded in `docs/STATUS.md` as of 2026-07-10 (PRE-STAGING CLOSURE atop G708) — not aspirational status. "Not proven" for staging/production means exactly that. G89-G707 and PRE-STAGING local fixes added local/no-IO foundations only; no integration claims staging or production live-proven status.

**PRE-STAGING CLOSURE (2026-07-10):** Local bug-scan + safe fixes + operator closeout reaffirm: staging/production columns remain **Not proven** for R2, email, GA/GSC, WordPress live draft, image generation, and target-env AI re-proof. Local PASS / disabled-safe / `configured_shape_ok` must not be promoted to staging, production, or Puriva Launch proof. OpenRouter remains **local controlled live only** (G77b). See [`PRE_STAGING_CLOSURE_VERDICT.md`](../operator/PRE_STAGING_CLOSURE_VERDICT.md).

**G78 docs audit (2026-07-10):** Monthly report metrics and WordPress tier labels refreshed in owned integration docs. Headline confirmations: (1) monthly reports — local baseline is MANUAL/snapshot placeholder (`placeholderOnly: true`); live GA/GSC is config-shape-only with no OAuth/token implementation; (2) WordPress — draft prep local-proven; live draft proof plan-only; publish frozen by default.

**G88 consolidation (2026-07-10):** Correct gate map recorded for G79-G88. G79 is implemented locally for monthly cap aggregation of live `COMPLETED` rows; G80 is policy-only (`actualCostUsd` remains `null` unless trusted provider cost is available); G81-G87 are planning/prep only. No staging/prod live proof, live AI call, email send, Google OAuth/sync, WordPress HTTP call, image-provider call, R2 IO, commit, push, or deploy was performed by this consolidation.

**G148 integration (2026-07-10):** Labels updated for G89-G147 lane outcomes: R2 no-IO readiness/proof stages/storage-key guards, notifications taxonomy/no-send foundation, GA/GSC helper/config foundation, WordPress draft payload + publish-freeze-before-fetch tests, image compliance policy helpers, Client Portal FINAL guards, pack/future-module contracts, AI budget reporting contract, and operator/security docs. Staging and production proof columns remain **Not proven** unless a prior recorded proof explicitly says otherwise.

**G228 integration (2026-07-10):** G149-G227 expanded local foundations (R2 proof-stage/redaction/URL policy, notification taxonomy V2 + persistence design, GA/GSC period/source-truth/output guards, WordPress slug/freeze/redaction/image-inclusion, image approval-loop helpers, Client Portal boundary tests, Puriva pack entitlements, future-module contracts, operator security/inventory/roadmap). **No staging or production proof column changes.** No live provider call, R2 IO, OAuth/sync, WordPress HTTP, email send, image-provider call, staging/prod probe, commit, push, or deploy was performed.

---

| Integration | Implementation status | Local proof | Staging proof | Production proof | Required secrets/env (names only) | Client / Puriva dependency | Risk | Next proof step | Owner approval required |
|---|---|---|---|---|---|---|---|---|---|
| **OpenRouter (AI text)** | Implemented; local deterministic gateway is default; OpenRouter path coded and config-validated; G79 local ledger aggregation includes live `COMPLETED` rows; **Orchestrator Lite staging AI-A = CONFIG SHAPE PROVEN** (no live call); **AI Delivery staging AI-B = STAGING LIVE PROVEN** (one bounded OpenRouter execute). **Role:** preferred text broker/adapter under AI Policy — not universal multi-modality boundary ([`AI_POLICY_PROVIDER_ROUTING.md`](../architecture/AI_POLICY_PROVIDER_ROUTING.md)) | PASS local deterministic (default) + **PASS local controlled live OpenRouter** (G71e/G77b; `anthropic/claude-haiku-4.5`; COMPLETED ledger row `5d8d635c-ced0-4a14-9b33-839e1fdee508`; G79 aggregation local unit proof; `actualCostUsd=null`) | Orchestrator: **CONFIG SHAPE PROVEN**; AI Delivery OpenRouter path: **STAGING LIVE PROVEN** (AI-B; marker `DCA-AI-B-20260712T071332Z-5vytpl`; ledger `a37577de-…`; estimated `$0.15`; actual null) | Not proven | `OPENROUTER_API_KEY`, `OPENROUTER_TEXT_PRIMARY_MODEL`, optional secondary/reviewer/long-context models, `AI_TEXT_GATEWAY=openrouter` | Puriva blocker: production live AI text re-proof still required; trusted invoice cost not proven; next staging gate WordPress (Image one-image already STAGING LIVE PROVEN) | Medium — cost/rate-limit exposure if enabled without budget guardrail review | Do not claim invoice cost until trusted `actualCostUsd` ingestion; production re-proof remains owner-gated; do not route images through OpenRouter text client | Yes |
| **Image generation** | Active OpenAI adapter under AI Policy; BFL retained inactive; **staging one-image path = STAGING LIVE PROVEN** on `c07df10` (`DCA-IMG-OPENAI-20260712T102457Z-a08x91rb`); three-image sets / regeneration / client approval / WordPress image delivery / production image **not** proven — [`AI_POLICY_PROVIDER_ROUTING.md`](../architecture/AI_POLICY_PROVIDER_ROUTING.md) · [`IMAGE_GENERATION_PROOF.md`](./IMAGE_GENERATION_PROOF.md) | PASS local adapter/guards + fake no-live smoke | **STAGING LIVE PROVEN** (one bounded OpenAI `gpt-image-1` image only; quality `low`; 1024×1024; submit=1; retry=0; fallback=false; BFL=0; COMPLETED ledger `e46ae5cd-…`; R2 N/A; live restored disabled) | Not proven | `IMAGE_GENERATION_*` (enabled + liveCallsAllowed default false); OpenAI key present on staging | Puriva blocker: three-image article set + R2 image bytes + client approval + WordPress attach still required | Medium — broader workflow not live-proven | Do not claim generic provider switching or BFL success; next WS7 = WordPress | Yes (broader surfaces) |
| **AI Delivery bounded content-to-draft execution bridge** | **STAGING CONNECTED WORKFLOW PROVEN** (2026-07-13) after local fake-provider proof — durable ledger + staging CLI; proof-only recipient override `25f846e`; product storageKey-only `/approve` gap closed in follow-on fix (local); R2 remains private | PASS (`test:ai-delivery-bounded-bridge:local` + `smoke:ai-delivery-bounded-bridge:local`) | **STAGING LIVE PROVEN** for connected prepare→Stage A→owner approve→Stage B→cleanup (`c0cc2bd5-…`); product API storageKey-only approve was **NOT_PROVEN** on that run and is closed locally pending staging re-proof | Not proven | `DCA_AI_DELIVERY_EXECUTION_TARGET`, exact IDs/manifest, image and WordPress dual live flags, `EMAIL_LIVE_SEND_AUTHORIZED`, `DCA_AI_DELIVERY_BOUNDED_PROOF_OWNER_RECIPIENT_EMAIL` (proof CLI only), provider credentials, R2 config, exact staging WordPress hostname | Connected staging workflow proven; product approve re-proof after deploy; production open | Medium — keep live flags false on staging API | Deploy approval-gap fix; run narrow product `/approve` proof only | Yes |
| **WordPress (draft-prep)** | Implemented and local-proven; G110-G113 add draft payload, credential-shape checks, and publish-freeze-before-fetch coverage; draft creation/handoff only | PASS (workflow brief + AI Delivery draft-prep smokes; focused lane tests 5/5) | Not proven | Not proven | N/A (draft-prep does not require publish env) | Required for Puriva Launch (draft/handoff only; auto-publish stays deferred) | Low for draft-prep; Medium for future publish | No action needed for draft-prep; live draft uses dedicated adapter (below) | No (draft-prep plan/docs); Yes (executing a live draft proof session or publish) |
| **WordPress (dedicated live-draft adapter)** | LOCAL IMPLEMENTED / FAKE-TRANSPORT PROVEN — `createWordPressDraft` + exact-ID trash; username field holds auth identifier; App Password encrypted; dual `WORDPRESS_DRAFT_LIVE_*` flags; generic publish remains frozen | PASS fake transport (`smoke:wordpress-live-draft-adapter:local`) | **STAGING LIVE PROVEN** (bounded one-draft create-and-trash only on `bd649d5`; marker `DCA-WP-DRAFT-20260712T130503Z-8ecfacb2`; post ID `6`; attempt TRASHED; retry=0; media=0; flags restored false) | Not proven | `WORDPRESS_DRAFT_LIVE_ENABLED`, `WORDPRESS_DRAFT_LIVE_CALLS_ALLOWED`, `CREDENTIAL_ENCRYPTION_MASTER_KEY`, PublicationTarget `siteUrl`+`wordpressUsername` (auth identifier; staging uses WordPress user e-mail), encrypted Application Password | Staging bounded draft path proven; general publication / image attach / production still open | Medium if live flags left enabled | Do not unfreeze generic publish; next WS7 = MI | Yes (broader surfaces) |
| **WordPress (live draft proof)** | Staging one-draft create-and-trash **STAGING LIVE PROVEN** (2026-07-12); image-attach and product publish remain separate | N/A (covered by dedicated adapter row) | **STAGING LIVE PROVEN** (bounded; no-image) | Not proven | Same as dedicated live-draft adapter row | Required Puriva draft/handoff staging proof satisfied for no-image create-and-trash only | Medium if over-claimed as full WP publish | Do not claim image attach, SEO metadata publish, or production WordPress | Yes (image attach / publish) |
| **WordPress (publish)** | Implemented behind a disabled-by-default gate; publish never occurs unless explicitly enabled | PASS disabled-safe (`smoke:wordpress-publish:local`); optional open-gate probe exists but is not the default | Not proven | Not proven | `WORDPRESS_PUBLISH_ENABLED=true`, `CREDENTIAL_ENCRYPTION_MASTER_KEY` | Explicitly deferred — auto-publish remains out of scope for Puriva Launch v1 | High if enabled prematurely (irreversible public content) | Do not schedule unless/until a separate explicit publish-proof block is approved | Yes |
| **GA4 / Google Search Console** | **WITHDRAWN** — owner decision 2026-07-13; live OAuth/sync/service-account/property-discovery/refresh-token/scheduled-sync not in scope; prior config helpers removed; MANUAL/snapshot monthly-report metrics remain; schema fields `gscSiteUrl`/`ga4PropertyId` retained for `SCHEMA_DEPRECATE_LATER` | N/A — not an active integration | N/A | N/A | **Not required** (`GA4_GSC_SYNC_ENABLED`, `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET` withdrawn) | **Not a Puriva blocker**; Monthly Report Flow v1 uses MANUAL/snapshot baseline | N/A — scope withdrawn | No action unless owner explicitly reopens live GA4/GSC | Owner reactivation only |
| **R2 (private storage)** | Implemented; disabled-safe by default; exact-key HEAD/DELETE on `4cd6d58`; SigV4 GET/PUT/HEAD/DELETE | PASS disabled-safe + **PASS staging live** one-object create/read/HEAD/delete/absence (`DCA-R2-20260712T081648Z-cc7ee7`; bucket `dcastaging`; sha256 `39b787cc…52a7`; 107 bytes pdf; `publicUrl=null`) | **STAGING LIVE PROVEN** (exact private-object IO only on `4cd6d58`) | Not proven | `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, optional `R2_ENDPOINT`/`R2_PUBLIC_BASE_URL` | Puriva blocker: production R2 + product-surface storage still required; image/client-deliverable/public delivery not claimed | Medium — production credential rotation still open | Next staging gate: MI (GA4/GSC **WITHDRAWN**); do not claim image-variant or client deliverable storage | Yes (production / broader surfaces) |
| **Email / notifications (Resend)** | Outbound email/outbox foundation + pure taxonomy/no-send adapters; **local v1 in-app inbox** (`InAppNotification` + list/mark-read APIs + shell panel) is implemented with payload redaction on write; local email mode reports `SKIPPED` without a real key; **staging provider acceptance proven once** on `a8a74e6` (2026-07-12) via adapter-only `AI_DELIVERY_APPROVED` — inbox/webhook delivery **not** claimed; live auth restored false — see [`EMAIL_NOTIFICATIONS_PROOF.md`](./EMAIL_NOTIFICATIONS_PROOF.md) §2 | PASS disabled-safe/no-send + local in-app persistence; staging one-send provider acceptance | **STAGING PROVIDER ACCEPTANCE PROVEN** (not inbox delivery) | Not proven | `RESEND_API_KEY` | Puriva blocker: "Transactional notification proof (in-system delivery wiring + email live proof)" | Medium — deliverability/reputation risk if unplanned fan-out; product risk if outbound email is mistaken for a fully wired approval-loop inbox | Prove approval-loop event → in-app row coverage; inbox/webhook receipt if claiming full staging live delivery; keep live auth disabled by default | Yes |
| **PDF / export (monthly reports, content plans)** | Implemented and local-proven; no external provider dependency for generation itself | PASS (`smoke:monthly-report:pdf`, `smoke:ai-seo-content-plan-pdf`) | Not proven (staging environment parity not re-verified) | Not proven | None beyond R2 for storage of the generated file | Required for Monthly Report Flow v1 (Puriva) — **local baseline uses MANUAL/snapshot placeholder metrics** | Low | Re-run the same local smokes once staging deploy proof exists for this commit | No (generation itself); Yes (if storage requires live R2) |
| **Monthly report metrics (MANUAL/snapshot — local baseline)** | Implemented and local-proven; admin snapshot import/approve path; Puriva manual placeholder metrics with `placeholderOnly: true` | PASS (`smoke:monthly-report:metrics`, `smoke:puriva-monthly-report:local`, `smoke:monthly-metrics-import:browser`) | Not proven | Not proven | None for manual path | Required for Puriva Launch local closeout; live GA4/GSC **WITHDRAWN** | Low if placeholder disclaimer preserved; High if presented as live traffic | Prove per `PURIVA_MANUAL_METRICS_V1_GATE.md`; never promote FINAL with placeholder totals without client-safe disclaimer | No (manual path) |
| **Google Docs / Drive live export** | Planning service implemented (`google-drive-export-planning.service.ts`); one guarded live-probe smoke exists; **separate from withdrawn GA4/GSC** | Guarded/optional (`smoke:google-drive-export-live:local`) — not run by default | Not proven | Not proven | Drive-specific Google OAuth credentials and scopes (not GA4/GSC env vars) | Deferred item (BLOK 9), not a hard Puriva blocker per current docs | Medium — OAuth scope creep risk | Confirm exact Drive scopes needed before any live OAuth consent is requested | Yes |
| **Credential encryption (WordPress-type credentials at rest)** | Implemented (`credential-encryption.service.ts`); has its own smoke | PASS (`smoke:credential-encryption:local`, `smoke:credential-master-key-probe:local`) | Not proven | Not proven | `CREDENTIAL_ENCRYPTION_MASTER_KEY` | Prerequisite for WordPress publish proof | Medium — key rotation/loss risk not yet exercised | Confirm key-rotation procedure before any production credential is ever encrypted with a production master key | Yes (before first real credential is stored) |

---

## Summary rules confirmed by code inspection

- Every integration follows the same **disabled → missing_config → configured_shape_ok** three-state pattern, and every readiness category explicitly self-labels as live-calls-deferred (`liveCallsDeferred: true` / equivalents) at the type level in `external-integrations-readiness.service.ts`.
- OpenRouter has **local** live-proven status (G77b) and **staging AI Delivery OpenRouter path STAGING LIVE PROVEN** (AI-B 2026-07-12); production live remains **Not proven**. Orchestrator Lite remains **CONFIG SHAPE PROVEN** only. Do not promote other integrations from local PASS to staging/production proof.
- Two gaps for a future small block (not blockers, just completeness): (1) image generation Phase B adds local `image_generation` readiness (`disabled` / `missing_config` / `configured_shape_ok`) but **no live provider client** — `configured_shape_ok` ≠ live-proven / not “image ready”; (2) email/Resend readiness is not yet exposed via the unified `GET /api/v1/integrations/readiness` endpoint — it is only visible via the email outbox smoke.
- **Email notifications audit (2026-07-09):** Full pathway/event audit completed in [`EMAIL_NOTIFICATIONS_PROOF.md`](./EMAIL_NOTIFICATIONS_PROOF.md). Headline finding: of the six Puriva-launch notification events (article ready, image set ready, client approved, client rejected, admin action required, monthly report final, WordPress draft prepared), only client-approved and client-rejected currently reach a real inbox; the rest are either internal-only (`SKIPPED`-only placeholder log) or entirely unwired. No live email was sent to produce that audit.
- **SEC-B1 (2026-07-09):** Legacy `/api/v1/briefs` cross-tenant IDOR fixed locally; regression test `briefs-tenant-boundary.integration.test.ts`. Not a launch integration row but a production safety prerequisite.
- **WordPress live draft proof (2026-07-09):** `WORDPRESS_DRAFT_PROOF.md` §6 documents a pre-execution plan only — no live WordPress call was made to produce it or this matrix update. Three concrete code/schema gaps must be explicitly decided (schema change vs. manual-check-only) before a live session can run: no `altText`/`caption`/`socialPreview` fields on `AiDeliveryArticleImage`, no idempotency/dedupe key on `PublicationLog`/`AiDeliveryWordPressPublishRequest`, and no code-level filter enforcing that only `AiDeliveryDeliverableImageApproval.status = APPROVED` images may attach to a draft.
- **GA/GSC withdrawal (2026-07-13):** Live GA4/GSC OAuth/sync is **WITHDRAWN** from current and planned scope — not a Puriva Launch blocker. `GA4_GSC_*` / `GOOGLE_OAUTH_*` env vars are **not required**. Google Drive remains a separate capability with its own OAuth needs. See [`MONTHLY_REPORT_LIVE_DATA_PROOF.md`](./MONTHLY_REPORT_LIVE_DATA_PROOF.md) for MANUAL/snapshot path.
- **Monthly report metrics tiers (G78, 2026-07-10; superseded 2026-07-13):** Local Puriva path uses MANUAL/snapshot placeholders (`PURIVA_MANUAL_METRICS_V1_GATE.md`, `placeholderOnly: true`). Live GA4/GSC is **WITHDRAWN** — do not represent manual placeholders as live analytics.
- **WordPress credential policy (G78, 2026-07-10):** Block 4 encryption foundation merged — encrypted Application Password save allowed locally; live HTTP and publish remain frozen per updated `WORDPRESS_CREDENTIAL_POLICY_DECISION.md` three-tier model.
- **G148 consolidation (2026-07-10):** G89-G147 local foundations are integrated, but do not change any staging/production proof column. R2, notifications/email, GA/GSC, WordPress, image generation, Client Portal staging QA, and production launch proofs remain owner-gated.
- **G228 consolidation (2026-07-10):** G149-G227 local foundations are integrated. Staging/production columns remain **Not proven** for R2, email, GA/GSC, WordPress live draft, image generation, and target-env AI re-proof. Historical next-gate sequence: [`G227_NEXT_30_GATES.md`](../operator/G227_NEXT_30_GATES.md).
- **G468 consolidation (2026-07-10):** G229-G467 local foundations are integrated (storage no-IO hardening, notification taxonomy/correlation design, GA/GSC/monthly guards, WordPress draft helpers, image compliance V3, Client Portal boundary tests, Puriva pack catalog, future-module contracts, AI budget/routing truth labels, security inventories, UI testability). Staging/production columns remain **Not proven** for R2, email, GA/GSC, WordPress live draft, image generation, and target-env AI re-proof. Historical next-gate sequence: [`G468_NEXT_50_GATES.md`](../operator/G468_NEXT_50_GATES.md).
- **G708 consolidation (2026-07-10):** G469-G707 local foundations are integrated across 20 lanes (R2/storage + private delivery, notification taxonomy, email no-send/templates, GA/GSC OAuth design + mapping, monthly output guards, WordPress draft helpers, image compliance, Client Portal/approval serializers, Puriva pack, future-module contracts, AI budget/orchestrator guards, security inventories, operator runbooks, UI testability, stale-claim sweep). Staging/production columns remain **Not proven** for R2, email, GA/GSC, WordPress live draft, image generation, and target-env AI re-proof. Next owner-gated sequence: [`G708_NEXT_GATES.md`](../operator/G708_NEXT_GATES.md).
- **G673–G684 stale-claim sweep (2026-07-10):** Docs-wide scan found no affirmative “live integration proven” / “email|Google|WordPress|R2|image|SaaS ready” product claims in this matrix. Do **not** promote local PASS / disabled-safe / config-shape rows to staging or production proof. **Superseded for AI Delivery OpenRouter staging path (2026-07-12 AI-B):** staging AI Delivery OpenRouter = **STAGING LIVE PROVEN**; production OpenRouter remains **Not proven**; Orchestrator remains config-shape only. **Superseded for R2 staging private-object path (2026-07-12):** staging R2 exact create/read/HEAD/delete/absence = **STAGING LIVE PROVEN** on `4cd6d58` / `dcastaging`; production R2 / image / client-deliverable / public delivery remain **Not proven**.

## Do NOT call external provider APIs with credentials

This document was produced entirely from code/config inspection. No OpenRouter, WordPress, GA/GSC, R2, or Resend API call was made while producing it.
